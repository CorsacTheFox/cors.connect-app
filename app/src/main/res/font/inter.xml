<?xml version="1.0" encoding="utf-8"?>
<!-- Inter — the Nocturne redesign's UI typeface (headings + body). Mono
     labels keep android:fontFamily="monospace" in their own layouts. -->
<font-family xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <font
        android:font="@font/inter_regular"
        android:fontWeight="400"
        android:fontStyle="normal"
        app:font="@font/inter_regular"
        app:fontWeight="400"
        app:fontStyle="normal" />
    <font
        android:font="@font/inter_medium"
        android:fontWeight="500"
        android:fontStyle="normal"
        app:font="@font/inter_medium"
        app:fontWeight="500"
        app:fontStyle="normal" />
    <font
        android:font="@font/inter_semibold"
        android:fontWeight="600"
        android:fontStyle="normal"
        app:font="@font/inter_semibold"
        app:fontWeight="600"
        app:fontStyle="normal" />
</font-family>
